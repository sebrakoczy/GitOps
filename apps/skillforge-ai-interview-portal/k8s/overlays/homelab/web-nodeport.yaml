apiVersion: v1
kind: Service
metadata:
  name: skillforge-web-nodeport
  namespace: skillforge
spec:
  type: NodePort
  selector:
    app: skillforge-web
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 32081
