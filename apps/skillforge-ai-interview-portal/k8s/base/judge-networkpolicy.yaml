apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: skillforge-judge-deny-egress
  namespace: skillforge
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: skillforge-judge
  policyTypes:
    - Egress
  egress: []
