apiVersion: v1
kind: Service
metadata:
  name: skillforge-web
  namespace: skillforge
spec:
  type: ClusterIP
  selector:
    app: skillforge-web
  ports:
    - name: http
      port: 8080
      targetPort: 8080
