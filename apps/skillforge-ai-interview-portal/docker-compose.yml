services:
  api:
    build: ./apps/api
    ports:
      - "8000:8000"
    env_file: .env.example
    volumes:
      - skillforge-data:/data
  web:
    build: ./apps/web
    ports:
      - "8080:8080"
    depends_on:
      - api
volumes:
  skillforge-data:
